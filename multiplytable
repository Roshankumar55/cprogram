#include<stdio.h>

int main()
{
    int num;
    printf("Enter your choose number ");
    scanf("%d",&num);
    printf("The multiplication table of %d\n",num);
    for (int i = 1; i < 11; i++)
    {
        printf("%d X %d = %d\n",num,i,i*num);
    }
    return 0;
}
